/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.cisco.product.app;

import com.cisco.product.list.LinkedList;

import static com.cisco.product.utilities.StringUtils.join;
import static com.cisco.product.utilities.StringUtils.split;
import static com.cisco.product.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
